apiVersion: v1
kind: Service
metadata:
  namespace: stats
  name: stats-ui-service
spec:
  type: NodePort
  selector:
    app: stats-ui
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      name: http
